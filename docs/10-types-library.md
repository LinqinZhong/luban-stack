# 类型库怎么用

类型库是项目内共享的数据结构定义，用来对齐「数据长什么样」。

## 用在哪里

- 数据池字段（对象/数组元素形状）  
- 组件属性、接口入参与返回  
- 服务方法参数与出参  

编辑器据此做选择、校验与导出时的类型信息。

## 使用建议

1. **跨页面重复的结构**（分页入参、商品、用户）进类型库，不要每个字段各写一份匿名 JSON。  
2. 改类型时想清楚兼容：前端绑定路径、后端接口会一起受影响。  
3. 界面表达式仍按对象字段访问；类型库约束的是形状，不是另一套语法。
