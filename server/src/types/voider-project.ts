/**
 * @deprecated 请从 `./luban-project.js` 导入。
 * 保留此文件以免旧 import 路径断裂。
 */
export {
  ENGINE_VERSION,
  DEFAULT_CANVAS_WIDTH,
  DEFAULT_PROJECT_VERSION,
  LUBAN_CONFIG_FILE,
  LEGACY_CONFIG_FILE,
  VOIDER_CONFIG_FILE,
  createDefaultConfig,
  isValidProjectConfig,
  type LubanProjectConfig,
  type VoiderProjectConfig,
} from './luban-project.js'
